# BD 攻堅名單 — 每日 Lead 生成專案

本專案透過純網路搜尋,每日為雲端/CDN/主機服務商產出高品質 BD Lead 日報(Markdown)。
目標行業:**AI、iGaming(博彩)、成人、賽事/體育(含電競、串流)**。

## 公司背景

> 以下為銷售判斷的核心依據,搜尋與評分時必須對照。

- **定位**:多雲方案經紀/整合商——阿里雲 BD 為主力,同時代理其他雲、機房、GPU 供應商與模型 API。**一個客戶談多種方案,組合成交**,不做單一方案思維。
- **產品線(四大類)**:
  1. Cloud 雲端 & Infra 基礎設施 & CDN & 直播 & DDoS 防禦
  2. GPU / AI 算力
  3. AI 解決方案、LLM、Vibe Coding(含成人內容 AI)、各家 AI 模型優惠折扣
  4. SMS 簡訊發送(OTP 驗證碼 / 行銷簡訊)
- **差異化優勢**:一站式多方案組合、折扣能力(多供應商比價)、敏感行業有解(博彩/成人配非主流供應商)、價格
- **目標地區**:台灣、香港、中國大陸、新加坡、馬來西亞;**城市顆粒度**——每筆 Lead 標注城市,便於同城打包拜訪
- **客單價/理想客戶規模**:月消費 USD 3,000 以上(單一方案未達標但**多方案組合可達標**者亦算)

### 方案映射表(每筆 Lead 必標 solutions,依此表)

| 客戶類型 | 可談方案(按優先順序) |
|---|---|
| iGaming 平台/營運商 | DDoS高防 → CDN → 雲主機 → SMS-OTP |
| 出海 AI 伴侶 / 成人 AI | GPU → 模型折扣 → CDN → SMS |
| 短劇 / 串流 / 直播平台 | CDN → 直播加速 → 雲主機 → SMS行銷 |
| AI 應用公司 | 模型折扣 → GPU → 雲主機 → CDN |
| 遊戲出海發行商 | CDN → 雲主機 → SMS-OTP |
| 賽事 / 電競串流 | 直播加速 → CDN → DDoS高防 |
| 合作夥伴 / ISV | 依其下游客群套用上表 |

### AI 線門檻放寬

因可轉售各家模型 API 折扣,**任何有 LLM token 消耗的公司皆為有效標的**(不限自購 GPU 級別);評分仍依訊號強度與地區判 A/B/C。

## Lead 三種類型

| 類型 | 定義 | 範例 |
|------|------|------|
| **客戶(Customer)** | 直接用量方,自己採購頻寬/算力/CDN/主機 | iGaming 平台、AI 應用公司、成人站、串流平台 |
| **合作夥伴(Partner)** | 本身不用量,但能持續轉介客戶 | 支付商、牌照顧問、廣告聯盟、行業展會主辦方 |
| **ISV(綁定型)** | 白牌平台/遊戲聚合商/風控 SaaS 等,綁定一家等於帶進整批下游客戶 | 白牌博彩平台商、遊戲 API 聚合商、串流技術方案商 |

## 硬性規則(不可違反)

1. **每個 Lead 必附來源連結**;無法提供可驗證來源的 Lead 一律不得產出,**不可捏造**任何公司、事件、數字。
2. **產出前先去重**:比對 `leads/leads.csv`(歷史名單)與 `config/exclusions.csv`(排除名單,如現有客戶),重複者不再列入日報(狀態有重大更新者除外,需註明)。
3. **評分標準**:
   - **A 級**:明確買方訊號 + 符合 ICP → **7 天內跟進**
   - **B 級**:符合 ICP 但訊號間接 → **2 週內跟進**
   - **C 級**:觀察名單 → 入庫(leads.csv)但不進日報重點區
4. **寧缺勿濫**:當日找不到足量合格 Lead 時,如實回報,不得湊數。

## 檔案結構

```
CLAUDE.md                    # 本檔:公司背景、規則、評分標準
.claude/commands/bd-daily.md # /bd-daily slash command 流程
config/icp.md                # 各行業買方訊號 + 跟進打法
config/search-queries.md     # 搜尋模板 + 行業媒體清單
config/exclusions.csv        # 排除名單(現有客戶等)
reports/_template.md         # 日報模板
reports/YYYY-MM-DD.md        # 每日產出
leads/leads.csv              # 累積 Lead 資料庫(去重依據)
scripts/run-daily.ps1        # Windows 每日排程腳本
```

## 日報產出要求

- 每日目標(加量模式):**A 級 3–5 筆、B 級 8–12 筆、C 級不設上限,總量 15–25 筆**。
- **A 級門檻不因加量放寬**:仍須近 7 天內明確買方訊號 + 符合 ICP。
- B 級來源可放寬為:近 14 天間接訊號,或「結構性符合 ICP」的名錄/展會/融資庫標的(如 SiGMA/SBC 參展平台商、行業目錄內的目標公司),須附名錄/來源連結。
- A 級每筆必含:公司、類型、行業、地區/城市、**可談方案組合(依映射表)**、訊號摘要、為何是現在、切入角色、開場話術、來源連結。
- 日報最上方為「**今日 5 件事**」行動清單:2–3 件跟進到期 + 2–3 件新開發,每件寫明「找誰、談什麼方案、用什麼管道」。
- 日報格式依 `reports/_template.md`。
